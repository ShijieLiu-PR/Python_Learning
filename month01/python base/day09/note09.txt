1. 实例变量
    a.语法
        --定义：对象.变量名
        --调用：对象.变量名
    b.说明
        --首次通过对象赋值为创建，再次赋值为修改。
        w01 = Wife()
        w01.name = "Lily"
        w01.name = "Lucy"
        --通常在构造函数(__init__)中创建
        w01 = Wife("Lily",24)
        print(w01.name)
        --每个对象存储一份，通过对象地址访问
    c.作用：描述所有对象的共有数据
    d.__dict__:对象的属性，用于存储自身实例变量的字典。

2. 实例方法
    a.语法
        --定义：
            def 方法名称（self, 参数列表）
                方法体
        --调用:
            对象地址.实例方法名(参数列表)
                不建议通过类名方法实例方法
    b.说明
        --至少有一个形参，第一个参数绑定调用这个方法的实例，一般命名为self
        --无论创建多少对象，方法只有一份，并且被所有方法共享。
    c.作用：表示对象行为

3. 类成员
4. 类方法
    定义：
        @classmethod
        def 方法名称(cls, 参数列表)
            方法体
    调用：
        类名.方法名（cls, 参数列表）
        不建议通过对象访问类方法
    说明：
        --至少一个形参，第一个形参用于绑定类，一般命名为cls
        --使用@classmethod修饰的目的是调用类方法时，传递隐式传递类
        --类方法中不能访问实例成员，实例方法中可以访问类成员。
    作用：
        访问类成员。


# 练习1：创建老婆类，随意实例化对象，统计老婆对象数量(定义方法)。
# 并画出内存图。

5. 静态方法
    语法：
        定义：
            @staticmethod
            def 方法名(参数列表):
                方法体
        调用:
            类名.方法名(参数列表)
            不建议通过对象访问静态方法
    说明：
        --使用@staticmethod修饰的目的是该方法不需要隐式传参数
        --静态方法不能访问实例成员和类成员

# 练习2：在二维列表中，获取23位置向左2个元素
# 练习3：在二维列表中，获取02位置向下2个元素
# 练习4：在二维列表中，获取20位置右上2个元素

6. 封装
    定义：
        1.数据角度：将一些基本数据类型复合成一个自定义类型。
                  复合人类的思考方式，将数据与对数据的操作封装起来。
        2.行为角度：向类外提供功能，隐藏实现细节
        3.设计角度：
            a.分而治之
                --将一个大的需求分解为许多类，每个类处理一个独立
                --拆分好处：
# 练习5：定义敌人类（姓名，攻击力，攻击速度（0-10），血量（0-100））
# 练习6：修改练习5的Enemy类，使用属性封装变量。

7.属性
    公开的实例变量，缺少逻辑验证。私有的实例变量与两个公开的方法相结合，操作略显复杂。而属性可以将两个方法的使用方式像操作变量一样方便。
    定义：
        @property
        def name(self)
            return self.__name

        @name.setter
        def name(self, value)
            self.__name = value
    调用：
        对象.属性名 = 数据
    说明：
    --通常两个公开的属性，保护一个私有的变量
    --@property负责读取，@属性名.setter 负责写入
    --只写，属性名=property(None,写入方法名)

day09作业：
1. 消化2048核心算法
2. 张三 教 李四 学习 python，李四 教 张三 玩游戏，张三 工作挣了 8000元， 李四工作挣了3000元。
    使用面向对象的思想描述以上场景。提示：对象与对象数据不同，类与类行为不同。
3. 创建技能类（技能名称，冷却时间，持续时间，攻击距离）
    要求：使用属性封装变量。
    创建技能列表：
    a.查找名称是“降龙十八掌”的技能对象
    b.查找持续时间大于10s的所有技能对象
    c.查找攻击距离最远的技能对象
    d.按照持续时间对列表升序排列